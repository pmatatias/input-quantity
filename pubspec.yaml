name: input_quantity
description: A widget to input quantity. Built with TextFormField. Type manually or increase and decrease value with the btn
version: 1.0.0
homepage: https://pmatatias.dev
repository: https://github.com/pmatatias/input-quantity

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
